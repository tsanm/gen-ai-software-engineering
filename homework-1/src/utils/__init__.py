"""Reusable helper utilities (string/PII masking, rate limiting, future date/number utils).

Small, self-contained routines that the other layers call but that carry no business
meaning of their own.
"""
