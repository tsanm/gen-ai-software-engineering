# Development & code-quality tooling (the local "SonarQube-equivalent" stack).
-r requirements.txt
ruff==0.6.*          # linting + formatting (style, bugs, imports)
mypy==1.*            # static type checking
bandit==1.*          # security static analysis
radon==6.*           # cyclomatic complexity + maintainability index
pytest-cov==5.*      # test coverage
